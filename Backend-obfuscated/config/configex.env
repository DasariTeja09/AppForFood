# Server Configuration (Safe to commit)
PORT=4000
NODE_ENV=DEVELOPMENT
FRONTEND_URL=http://localhost:3000

# Database (Use local DB for development)
DB_LOCAL_URI=mongodb://127.0.0.1:27017/Internship

# JWT (Replace with your own secret in production)
JWT_SECRET=your_jwt_secret_here_use_long_random_string
JWT_EXPIRES_TIME=90

# Cloudinary (Replace with your actual credentials)
CLOUDINARY_CLOUD_NAME=your_cloud_name_here
CLOUDINARY_API_KEY=your_api_key_here
CLOUDINARY_API_SECRET=your_api_secret_here

# Mailtrap (Replace with your test credentials)
EMAIL_USERNAME=your_mailtrap_username
EMAIL_PASSWORD=your_mailtrap_password
EMAIL_HOST=sandbox.smtp.mailtrap.io
EMAIL_PORT=25
EMAIL_FROM=

# Stripe (NEVER commit real keys! Replace these)
STRIPE_SECRET_KEY=your_stripe_secret_key_here
STRIPE_API_KEY=your_stripe_publishable_key_here